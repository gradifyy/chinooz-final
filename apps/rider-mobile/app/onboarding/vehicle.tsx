import VehicleStepScreen from '../../components/VehicleStepScreen'

/**
 * RO4 — Vehicle details step (onboarding step 2).
 * Renders the shared VehicleStepScreen with vehicle-type cards,
 * conditional fields, and dirty-guard navigation.
 */
export default function VehicleRoute() {
  return <VehicleStepScreen />
}
