import DocumentStepScreen from '../../components/DocumentStepScreen'

/**
 * RO5 — Document / KYC upload step (onboarding step 3).
 * Renders the shared DocumentStepScreen with per-vehicle-type required
 * uploads, status chips, capture/replace, trust copy, and validation.
 */
export default function DocumentsRoute() {
  return <DocumentStepScreen />
}
