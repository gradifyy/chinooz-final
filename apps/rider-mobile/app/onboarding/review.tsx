import ReviewStepScreen from '../../components/ReviewStepScreen'

/**
 * RO6 — Review & submit step (onboarding step 4).
 * Renders the shared ReviewStepScreen with summary cards, consent checkboxes,
 * and mock submission to pending state via TanStack Query.
 */
export default function ReviewRoute() {
  return <ReviewStepScreen />
}
