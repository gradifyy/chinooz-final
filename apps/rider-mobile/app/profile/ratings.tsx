import React from 'react'
import RatingsFeedbackScreen from '../../components/RatingsFeedbackScreen'

/**
 * RP3 — Ratings & feedback view route.
 * Reachable from the Performance "Ratings & feedback" entry point.
 * Pushed from Profile, NOT a bottom tab.
 */
export default function RatingsRoute() {
  return <RatingsFeedbackScreen />
}
