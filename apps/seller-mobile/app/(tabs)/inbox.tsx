import React from 'react'
import SellerMessagesScreen from '../messages'

// Inbox tab — renders the seller messages screen inside the tab shell.
export default function InboxTab() {
  return <SellerMessagesScreen />
}
