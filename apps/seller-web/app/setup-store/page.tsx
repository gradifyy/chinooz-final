import StoreStepClient from '@/components/StoreStepClient'

export const metadata = {
  title: 'Store Setup — Chinooz Seller',
}

export default function SetupStorePage() {
  return <StoreStepClient />
}
