import BusinessStepClient from '@/components/BusinessStepClient'

export const metadata = {
  title: 'Business & KYC — Chinooz Seller',
}

export default function SetupBusinessPage() {
  return <BusinessStepClient />
}
