import ReviewStepClient from '@/components/ReviewStepClient'

export const metadata = {
  title: 'Review & Submit — Chinooz Seller',
}

export default function SetupReviewPage() {
  return <ReviewStepClient />
}
